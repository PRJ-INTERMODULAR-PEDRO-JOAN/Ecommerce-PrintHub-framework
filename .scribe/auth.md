# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer {AQUÍ_PONDRÁS_TU_TOKEN}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

Puedes generar un token iniciando sesión y pegarlo aquí para probar los endpoints protegidos.
